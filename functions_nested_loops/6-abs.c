#include "main.h"

/**
 * _abs - n is integer
 * @n : caractere n
 *
 * Return: number if number is pisitve -number otherwise
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
